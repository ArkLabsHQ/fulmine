{
  "name": "fulmine-web-e2e",
  "private": true,
  "description": "Playwright e2e tests for the Fulmine web UI. Run against the arkade-regtest stack (make regtest-up) via `make web-e2e`.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "install-browser": "playwright install --with-deps chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
